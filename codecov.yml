# reference: https://docs.codecov.io/docs/codecovyml-reference
---

codecov:
  # Token is configured in the Codecov UI / CI secrets — do not commit tokens.

coverage:
  status:
    patch:
      default:
        target: 100%  # require patches to be 100%
    project: false
comment: false
